kig:
- should synth merge of make_kdeedu_cool to trunk (217425 -> 217432)

marble:
- synth merge of marble-geodata-nt to trunk (946595 -> 946666)

blinken:
- docs are missing in history of version branches

kalgebra:
- missing docs from playground and kdereview history

rocs:
- missing docs from playground and kdereview history
- 4.6 branching has docs in root, deletes all other files

kstars:
- there were some deletions and re-additions of the entire trunk,
  should handle sanely in git.
- synth merge of make_kdeedu_cool to trunk (190438 -> 191485)

parley:
- everything seems fine :)

libkdeedu:
- the 3.4, 4.3, 4.4 branches have a single commit; check in SVN if
  this is correct
- weird stuff in 757794, it's a giant commit, apparently a feature
  branch merge, but I don't know which branch

cantor:
- docs missing from playground and kdereview history, under both the
  "kantor" and "mathematik" names
- there may be a "soc branch" that isn't being converted (see r1130956
  for a possible backport from it)
